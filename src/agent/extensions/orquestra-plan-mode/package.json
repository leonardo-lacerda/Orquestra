{
  "name": "orquestra-plan-mode",
  "description": "Plan mode for the Orquestra agent panel. Toggle with /plan: the agent investigates read-only (dispatching scout subagents in parallel) and proposes a structured plan via the plan_complete tool. /apply-plan re-enables writes.",
  "private": true,
  "pi": {
    "extensions": ["./index.ts"]
  }
}
